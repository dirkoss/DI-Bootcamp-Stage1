# Use .get(x) if you want to search for a key in the dictionary and return None if it doesn't exist.
#   You can put an alternative (default), if it doesnt exist.
#   If it does exist it will return the value and replace the default.
